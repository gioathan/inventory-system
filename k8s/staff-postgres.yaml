apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-staff-data
  namespace: inventory-system
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-staff
  namespace: inventory-system
spec:
  replicas: 1 # see catalog-postgres.yaml for why this is a Deployment, not a StatefulSet
  selector:
    matchLabels:
      app: postgres-staff
  template:
    metadata:
      labels:
        app: postgres-staff
    spec:
      containers:
        - name: postgres
          image: postgres:18.3
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: staffdb
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_HOST_AUTH_METHOD
              value: scram-sha-256
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql # postgres:18+ layout — see catalog-postgres.yaml
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-staff-data
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-staff
  namespace: inventory-system
spec:
  selector:
    app: postgres-staff
  ports:
    - port: 5432
      targetPort: 5432
