apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-api
  namespace: inventory-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard-api
  template:
    metadata:
      labels:
        app: dashboard-api
    spec:
      containers:
        - name: dashboard-api
          image: dashboard-api:dev
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: ASPNETCORE_HTTP_PORTS
              value: "8080"
            # Same override as scan-gateway.yaml — see that file's comment.
            - name: GrpcClients__CatalogApi
              value: "http://catalog-api"
            - name: GrpcClients__InventoryApi
              value: "http://inventory-api"
            - name: Jwt__SigningKey
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: Jwt__SigningKey
            # Traces/metrics ship here via OTLP/gRPC — see k8s/jaeger.yaml.
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://jaeger:4317"
            # See catalog-api.yaml for why this is needed — Aspire sets it automatically locally.
            - name: OTEL_SERVICE_NAME
              value: dashboard-api
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /alive
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: dashboard-api
  namespace: inventory-system
spec:
  selector:
    app: dashboard-api
  ports:
    - name: http
      port: 80
      targetPort: http
